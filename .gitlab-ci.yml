image: node:latest

cache:
  paths:
   - node_modules/

before_script:
  - npm install

stages:
  - build
  - test
  - deploy

build:
  stage: test
  script: npm run test

deploy:
  stage: deploy
  only:
   - master
  script:
   - npm run deploy-initial
